export enum CharacterStatusEnum {
  ALIVE = "Alive",
  DEAD = "Dead",
}

export enum CharacterSpeciesEnum {
  HUMAN = "Human",
}

export enum CharacterGenderEnum {
  MALE = "Male",
  FEMALE = "Female",
}
